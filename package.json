{
    "dependencies": {
        "ical.js": "^2.2.1"
    },
    "scripts": {
        "format": "prettier --write .",
        "build": "extension build --browser=firefox --zip",
        "dev": "extension dev --browser=firefox",
        "preview": "extension preview --browser=firefox"
    },
    "devDependencies": {
        "extension": "^2.1.3",
        "prettier": "^3.5.3",
        "web-ext": "^8.6.0"
    }
}
